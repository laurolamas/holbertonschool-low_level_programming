#include <stdio.h>
/**
 * main - wefwargwea101a113
 * Return: 0
 */
int main(void)
{
	char ch;

	for (ch = '0' ; ch <= 'f' ; ch++)
	{
		if (!(ch > '9' && ch < 'a'))
			putchar (ch);
	}
	putchar ('\n');

return (0);
}
