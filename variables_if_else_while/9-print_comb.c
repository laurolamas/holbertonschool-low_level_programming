#include <stdio.h>
/**
 * main - wefwargwe
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = '0' ; ch <= '9' ; ch++)
	{
		if (ch == '9')
		{
			putchar (ch);
		}
		else
		{
			putchar (ch);
			putchar (',');
			putchar (' ');
		}
	}
	putchar ('\n');

return (0);
}
